export type TSales = {
  month: string;
  totalSales: number;
  totalRevenue: number;
};

export type TSalesCategory = {
  month: string;
  category: string;
  totalSales: number;
  totalRevenue: number;
};

export type TSalesProduct = {
  month: string;
  product: string;
  totalSales: number;
  totalRevenue: number;
};
